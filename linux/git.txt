
# windows ssh key
ssh-keygen -t rsa -C "nmgyeyun@126.com"
# 默认生成key路径
C:\Users\USERNAME\.ssh



git config -- global http.sslBackend "openssl"

git config --global user.name "nmgyeyun"
git config --global user.email "nmgyeyun@126.com"


# config
git config --global credential.helper store

git config --global alias.co checkout
git config --global alias.ci commit
git config --global alias.br branch
git config --global alias.ba "branch -a"
git config --global alias.st status
git config --global alias.pl pull
git config --global alias.ps push
git config --global alias.rtv "remote -v"
git config --global alias.psom "push origin master"
git config --global http.postBuffer 1073741824
git config --global http.sslVerify "false"


==============================================================================================================
# 设置代理

git config --global http.proxy 127.0.0.1:7890
git config --global https.proxy 127.0.0.1:7890

# 取消代理
git config --global --unset http.proxy
git config --global --unset https.proxy

==============================================================================================================
cat .gitconfig 
[credential]
        helper = store
[user]
        email = nmgyeyun@126.com
        name = Xiaocheng Wang
[core]
        editor = vim

[alias]
    co = checkout
    ci = commit
    br = branch
    ba = branch -a
    st = status
    pl = pull
    ps = push
    psom = push origin master 


ssh-keygen -t rsa -C "nmgyeyun@126.com" -b 4096
ssh-keygen -t rsa -C `hostname` -b 4096


==============================================================================================================
常见问题：
1. 未能创建 SSL/TLS 安全通道
[sangfor@msys note]$git ps
fatal: 发送请求时出错。
fatal: 请求被中止: 未能创建 SSL/TLS 安全通道。
error: unable to read askpass response from 'C:/Program Files/Git/mingw64/bin/git-askpass.exe'

解决：
git clone git@github.com:nmgyeyun/note.git 
